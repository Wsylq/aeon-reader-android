package com.aeonreader

import org.junit.Test
import org.junit.Assert.assertTrue

/**
 * Smoke test to verify the project setup compiles and basic JVM tests run.
 */
class ProjectSetupTest {

    @Test
    fun `project compiles and JVM tests run`() {
        assertTrue("Project setup is valid", true)
    }
}
